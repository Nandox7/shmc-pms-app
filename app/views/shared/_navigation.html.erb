<nav class="navbar navbar-default" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">SHC-PMS Application</a>
    </div>
    
    <% if user_signed_in? %>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Home', home_index_path %></li>
        <% if !current_user.profile.nil? && current_user.profile.is_admin %>
          <li><%= link_to 'Users Profile', profiles_path %></li>
        <% end %>
        <li><%= link_to 'Patients', patients_path %></li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Appointments<b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li><%= link_to 'Today', appointments_path(:range => 'today') %></li>
                <li><%= link_to 'Week', appointments_path(:range => 'week')  %></li>
                <li><%= link_to 'All', appointments_path %></li>
            </ul>
        </li>
      </ul>
      <% end %>
      
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
           <li><%= link_to 'My Profile', edit_profile_path(current_user.id) %></li>
           <li><%= link_to 'Sign out', destroy_user_session_path, :method => :delete%></li>
        <% else %>
          <li><%= link_to 'Register', new_user_registration_path %></li>
          <li><%= link_to 'Sign in', new_user_session_path %></li>
        <% end %>
      </ul>
    </div>

  </div>
</nav>
